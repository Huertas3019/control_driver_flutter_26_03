rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permitir todo por ahora para pruebas (Expira en 30 días)
      allow read, write: if request.time < timestamp.date(2026, 4, 16);
    }
  }
}
